"""CRE Arbitrage Engine components.

Entry point is `main.py` in the project root, which must be run from the
project root so that ConfigManager resolves `config.yaml` and python-dotenv
resolves `.env`.
"""
